'use client';
import { LoginForm } from '@/components/login-form';

const Signin = () => {
  return (
    <main className='mb-10 mt-20'>
      <LoginForm />
    </main>
  );
};

export default Signin;
